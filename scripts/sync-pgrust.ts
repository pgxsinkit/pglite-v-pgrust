/**
 * Populate `src/vendor/pgrust/` and `public/pgrust/` from a sibling pgrust checkout, and write
 * `src/vendor/pgrust/VERSION` with the synced commit so the environment header can report it.
 *
 * Stub: the pgrust Engine is wired up in a later slice.
 */

console.error("sync:pgrust is not implemented in this slice");
process.exit(1);
